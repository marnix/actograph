{
  "name": "actograph",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "build:verify": "npm run clean && tsc --build && node dist/index.js --version",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "acto": "npm run build && ./acto",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "concurrently --success all 'npm:typecheck' 'npm:format:check' 'npm:test'",
    "ci": "npm run build:verify && npm run check",
    "clean": "node -e \"import('fs').then(f=>{f.rmSync('dist',{recursive:true,force:true})})\""
  },
  "dependencies": {
    "@automerge/automerge": "^3.2.6",
    "commander": "^15.0.0",
    "cuss": "^2.2.0",
    "graphology": "^0.26.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "concurrently": "^10.0.3",
    "prettier": "^3.8.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
